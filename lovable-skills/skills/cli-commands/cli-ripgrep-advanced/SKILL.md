---
name: cli-ripgrep-advanced
description: Use when working with ripgrep (rg) advanced. Always uses the correct, idiomatic commands: search, regex, files, types, replace. Not for guessing syntax — provide actual commands.
---

# CLI: ripgrep (rg) advanced

Quick reference for ripgrep (rg) advanced.

## Most-Used Commands
search, regex, files, types, replace

## Command Reference
For each command:
- Syntax
- Common flags
- Examples
- Gotchas

## Common Workflows
Document 3-5 real-world workflows using these commands.

## Tips & Tricks
- Aliases
- Shell completion
- Common pitfalls

## Cross-Platform Notes
- macOS / Linux / Windows differences
- Version-specific features

## Forbidden Patterns
- Don't pipe to less when a flag exists
- Don't grep when built-in filter exists
- Don't write scripts for what the CLI already does

## Output Format
1. Command(s) to run
2. Explanation
3. Expected output
4. Troubleshooting notes

