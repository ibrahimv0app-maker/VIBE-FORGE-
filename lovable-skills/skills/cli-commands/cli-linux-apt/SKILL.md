---
name: cli-linux-apt
description: Use when working with apt (Debian/Ubuntu). Always uses the correct, idiomatic commands: install, update, upgrade, remove, autoremove. Not for guessing syntax — provide actual commands.
---

# CLI: apt (Debian/Ubuntu)

Quick reference for apt (Debian/Ubuntu).

## Most-Used Commands
install, update, upgrade, remove, autoremove

## Command Reference
For each command:
- Syntax
- Common flags
- Examples
- Gotchas

## Common Workflows
Document 3-5 real-world workflows using these commands.

## Tips & Tricks
- Aliases
- Shell completion
- Common pitfalls

## Cross-Platform Notes
- macOS / Linux / Windows differences
- Version-specific features

## Forbidden Patterns
- Don't pipe to less when a flag exists
- Don't grep when built-in filter exists
- Don't write scripts for what the CLI already does

## Output Format
1. Command(s) to run
2. Explanation
3. Expected output
4. Troubleshooting notes

